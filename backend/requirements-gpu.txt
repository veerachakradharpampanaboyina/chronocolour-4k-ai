# ============================================================
# ChronoColor 4K AI — GPU-Specific Dependencies
# ============================================================
# These are installed ONLY in the GPU worker container.
# PyTorch is pre-installed in the CUDA base image.
# ============================================================

# --- AI Frameworks ---
basicsr==1.4.2
facexlib==0.3.0
gfpgan==1.3.8
realesrgan==0.3.0

# --- Detection & Segmentation ---
ultralytics==8.3.57

# --- Optical Flow ---
# RAFT is included via torchvision.models.optical_flow

# --- Colorization ---
# DDColor and ControlNet installed from git repos in Dockerfile

# --- GPU Utilities ---
pynvml==11.5.3
GPUtil==1.4.0
